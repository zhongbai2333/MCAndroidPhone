# 早期适配器已退役

本项目已迁为独立手机 Mod，不再依赖 NCPB 或 NetMusic。旧链接保留在此，当前构建、安装与操作请参阅 [独立版说明](standalone.md)。

升级时用 `mcandroidphone-0.1.0-prototype.jar` 替换旧的 `mcandroidphone-ncpb-adapter-*.jar`。两者具有相同的 mod ID，不要同时安装。手机物品 ID 保持 `mcandroidphone:android_phone`。
